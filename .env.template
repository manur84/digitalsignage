# GitHub Authentication for pushing to main branch
#
# The Git proxy blocks direct pushes to main with HTTP 403.
# To push your 37 local commits to origin/main, you need to provide a GitHub token.
#
# Steps:
# 1. Create a GitHub Personal Access Token at: https://github.com/settings/tokens
#    - Select scope: repo (Full control of private repositories)
# 2. Copy this file to .env: cp .env.template .env
# 3. Replace YOUR_GITHUB_TOKEN_HERE with your actual token
# 4. Run: chmod 600 .env  (protect the file)
# 5. Push with: source .env && git push https://token:${GITHUBTOKEN}@github.com/manur84/digitalsignage.git main
#
# NOTE: .env is in .gitignore and will NOT be committed to the repository

GITHUBTOKEN=YOUR_GITHUB_TOKEN_HERE
GITHUBREPO=https://github.com/manur84/digitalsignage.git
