<UserControl x:Class="DigitalSignage.Server.Views.MobileAppManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:DigitalSignage.Server.Converters"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <!-- Converters -->
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityStandard"/>

        <!-- App Registration Status Color Converter -->
        <converters:AppRegistrationStatusToColorConverter x:Key="AppStatusToColor"/>

        <!-- Modern Action Button Style (Inline in DataGrid) -->
        <Style x:Key="InlineActionButton" TargetType="Button">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="3,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="MinWidth" Value="75"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Header Button Style -->
        <Style x:Key="HeaderButton" TargetType="Button">
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Modern Header -->
        <Border Grid.Row="0"
                Background="#2196F3"
                CornerRadius="8,8,0,0"
                Padding="24,16"
                Margin="0,0,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Mobile App Registrations"
                              FontSize="22"
                              FontWeight="SemiBold"
                              Foreground="White"/>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Foreground="#BBDEFB" FontSize="13" Margin="0,0,20,0">
                            <Run Text="📱"/>
                            <Run Text="{Binding Registrations.Count, Mode=OneWay}"/>
                            <Run Text="Total"/>
                        </TextBlock>
                        <Border Background="#FF9800"
                                CornerRadius="10"
                                Padding="8,3"
                                VerticalAlignment="Center"
                                Visibility="{Binding PendingCount, Converter={StaticResource BoolToVisibilityStandard}}">
                            <TextBlock Foreground="White" FontWeight="SemiBold" FontSize="12">
                                <Run Text="⏳"/>
                                <Run Text="{Binding PendingCount, Mode=OneWay}"/>
                                <Run Text="Pending"/>
                            </TextBlock>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <!-- Refresh Button -->
                <Button Grid.Column="1"
                        Content="🔄 Refresh"
                        Command="{Binding LoadRegistrationsCommand}"
                        Background="#1976D2"
                        Style="{StaticResource HeaderButton}"/>
            </Grid>
        </Border>

        <!-- App List -->
        <Border Grid.Row="1"
                BorderBrush="#E0E0E0"
                BorderThickness="1,0,1,1"
                CornerRadius="0,0,8,8"
                Background="White">
            <DataGrid ItemsSource="{Binding Registrations}"
                      SelectedItem="{Binding SelectedRegistration, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      SelectionMode="Single"
                      IsReadOnly="True"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      RowHeight="60"
                      FontSize="13"
                      AlternatingRowBackground="#FAFAFA"
                      RowBackground="White"
                      CanUserResizeRows="False"
                      CanUserReorderColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#2196F3"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Padding" Value="12,12"/>
                        <Setter Property="BorderBrush" Value="#1976D2"/>
                        <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        <Setter Property="Height" Value="42"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="Margin" Value="0,1"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#1976D2"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <!-- Device Name -->
                    <DataGridTextColumn Header="Device Name"
                                       Binding="{Binding DeviceName}"
                                       Width="*"
                                       MinWidth="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Medium"/>
                                <Setter Property="FontSize" Value="13"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Platform -->
                    <DataGridTextColumn Header="Platform"
                                       Binding="{Binding Platform}"
                                       Width="90"/>

                    <!-- Status Badge -->
                    <DataGridTemplateColumn Header="Status" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12"
                                       Padding="10,5"
                                       HorizontalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                    <Setter Property="Background" Value="#FFF3E0"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Approved">
                                                    <Setter Property="Background" Value="#E8F5E9"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                    <Setter Property="Background" Value="#FFEBEE"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Revoked">
                                                    <Setter Property="Background" Value="#F5F5F5"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Status}"
                                              FontWeight="SemiBold"
                                              FontSize="11"
                                              HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                        <Setter Property="Foreground" Value="#F57C00"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Approved">
                                                        <Setter Property="Foreground" Value="#388E3C"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                        <Setter Property="Foreground" Value="#D32F2F"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Revoked">
                                                        <Setter Property="Foreground" Value="#757575"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Registered At -->
                    <DataGridTextColumn Header="Registered"
                                       Binding="{Binding RegisteredAt, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}"
                                       Width="130">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="12"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Action Buttons -->
                    <DataGridTemplateColumn Header="Actions" Width="340">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center">

                                    <!-- Approve Button (only for Pending) -->
                                    <Button Command="{Binding DataContext.ApproveCommand,
                                                     RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           ToolTip="Approve this device">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource InlineActionButton}">
                                                <Setter Property="Background" Value="#4CAF50"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#388E3C"/>
                                                    </Trigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Approved">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Revoked">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="✓" Margin="0,0,4,0" FontSize="13"/>
                                            <TextBlock Text="Approve"/>
                                        </StackPanel>
                                    </Button>

                                    <!-- Reject Button (only for Pending) -->
                                    <Button Command="{Binding DataContext.RejectCommand,
                                                     RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           ToolTip="Reject this device">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource InlineActionButton}">
                                                <Setter Property="Background" Value="#F44336"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#D32F2F"/>
                                                    </Trigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Approved">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Revoked">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="✗" Margin="0,0,4,0" FontSize="13"/>
                                            <TextBlock Text="Reject"/>
                                        </StackPanel>
                                    </Button>

                                    <!-- Revoke Button (only for Approved) -->
                                    <Button Command="{Binding DataContext.RevokeCommand,
                                                     RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           ToolTip="Revoke access for this device">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource InlineActionButton}">
                                                <Setter Property="Background" Value="#FF9800"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F57C00"/>
                                                    </Trigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Revoked">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⊗" Margin="0,0,4,0" FontSize="13"/>
                                            <TextBlock Text="Revoke"/>
                                        </StackPanel>
                                    </Button>

                                    <!-- Edit/Details Button (for Approved) -->
                                    <Button ToolTip="View details and edit permissions">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource InlineActionButton}">
                                                <Setter Property="Background" Value="#2196F3"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#1976D2"/>
                                                    </Trigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Revoked">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⚙" Margin="0,0,4,0" FontSize="13"/>
                                            <TextBlock Text="Edit"/>
                                        </StackPanel>
                                    </Button>

                                    <!-- Delete Button -->
                                    <Button Command="{Binding DataContext.DeleteCommand,
                                                     RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           ToolTip="Delete this registration">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource InlineActionButton}">
                                                <Setter Property="Background" Value="#757575"/>
                                                <Setter Property="MinWidth" Value="65"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#616161"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🗑" Margin="0,0,4,0"/>
                                            <TextBlock Text="Delete"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Details Panel (Permissions Editor) -->
        <Border Grid.Row="2"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="8"
                Background="White"
                Padding="24"
                Margin="0,16,0,0"
                Visibility="{Binding SelectedRegistration, Converter={StaticResource BoolToVisibilityStandard}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock FontWeight="SemiBold" FontSize="15" Foreground="#333333" Margin="0,0,0,12">
                        <Run Text="📱"/>
                        <Run Text="{Binding SelectedRegistration.DeviceName, Mode=OneWay}"/>
                        <Run Text="-" Foreground="#999999"/>
                        <Run Text="Permissions" Foreground="#999999"/>
                    </TextBlock>

                    <StackPanel Orientation="Horizontal">
                        <CheckBox Content="👁 View (Devices, Layouts, Screenshots)"
                                 IsChecked="{Binding ViewPermission, Mode=TwoWay}"
                                 FontSize="13"
                                 VerticalAlignment="Center"
                                 Margin="0,0,20,0"/>

                        <CheckBox Content="🎮 Control (Restart, Commands, Volume)"
                                 IsChecked="{Binding ControlPermission, Mode=TwoWay}"
                                 FontSize="13"
                                 VerticalAlignment="Center"
                                 Margin="0,0,20,0"/>

                        <CheckBox Content="⚙ Manage (Assign Layouts, Schedules)"
                                 IsChecked="{Binding ManagePermission, Mode=TwoWay}"
                                 FontSize="13"
                                 VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock FontSize="11" Foreground="#999999" Margin="0,10,0,0">
                        <Run Text="Device ID:"/>
                        <Run Text="{Binding SelectedRegistration.DeviceIdentifier, Mode=OneWay}"/>
                        <Run Text="  •  Platform:"/>
                        <Run Text="{Binding SelectedRegistration.Platform, Mode=OneWay}"/>
                        <Run Text="  •  Version:"/>
                        <Run Text="{Binding SelectedRegistration.AppVersion, Mode=OneWay}"/>
                    </TextBlock>
                </StackPanel>

                <!-- Save Button -->
                <Button Grid.Column="1"
                       Content="💾 Save Permissions"
                       Command="{Binding ApproveCommand}"
                       Background="#4CAF50"
                       Foreground="White"
                       FontWeight="SemiBold"
                       FontSize="13"
                       Padding="20,10"
                       Margin="20,0,0,0"
                       VerticalAlignment="Center"
                       Cursor="Hand"
                       ToolTip="Apply permission changes">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#4CAF50"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                               CornerRadius="4"
                                               Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#388E3C"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                    <Setter Property="Cursor" Value="Arrow"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3"
                Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityStandard}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Loading..."
                           Foreground="White"
                           FontSize="18"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
