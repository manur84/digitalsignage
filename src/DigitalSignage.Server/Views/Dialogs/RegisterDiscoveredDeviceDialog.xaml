<Window x:Class="DigitalSignage.Server.Views.Dialogs.RegisterDiscoveredDeviceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Register Discovered Device"
        Height="550" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Register Device" FontSize="24" FontWeight="Bold" Margin="0,0,0,8"/>
            <TextBlock Text="Fill in the details to register this discovered device"
                       FontSize="12" Foreground="#666666" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Discovered Information -->
                <TextBlock Text="Discovered Information" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12"/>
                <Border Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Hostname:" FontWeight="SemiBold" Margin="0,0,12,8"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="DiscoveredHostname" Text="" Margin="0,0,0,8"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="IP Address:" FontWeight="SemiBold" Margin="0,0,12,8"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="DiscoveredIpAddress" Text="" Margin="0,0,0,8"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="MAC Address:" FontWeight="SemiBold" Margin="0,0,12,8"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="DiscoveredMacAddress" Text="" Margin="0,0,0,8"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Discovery Method:" FontWeight="SemiBold" Margin="0,0,12,0"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" x:Name="DiscoveryMethod" Text=""/>
                    </Grid>
                </Border>

                <!-- Registration Details -->
                <TextBlock Text="Registration Details" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12"/>
                <Border Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="4" Padding="12">
                    <StackPanel>
                        <TextBlock Text="Device Name *" Margin="0,0,0,4" FontWeight="SemiBold"/>
                        <TextBox x:Name="DeviceNameTextBox" Margin="0,0,0,12" Padding="8"/>

                        <TextBlock Text="Description" Margin="0,0,0,4" FontWeight="SemiBold"/>
                        <TextBox x:Name="DescriptionTextBox" Margin="0,0,0,12" Padding="8" Height="60"
                                 TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

                        <TextBlock Text="Group" Margin="0,0,0,4" FontWeight="SemiBold"/>
                        <TextBox x:Name="GroupTextBox" Margin="0,0,0,12" Padding="8"/>

                        <TextBlock Text="Location" Margin="0,0,0,4" FontWeight="SemiBold"/>
                        <TextBox x:Name="LocationTextBox" Margin="0,0,0,12" Padding="8"/>

                        <TextBlock Text="Registration Token *" Margin="0,0,0,4" FontWeight="SemiBold"/>
                        <TextBox x:Name="TokenTextBox" Margin="0,0,0,8" Padding="8"/>
                        <TextBlock Text="Leave empty to use default token from settings"
                                   FontSize="10" Foreground="#666666" Margin="0,0,0,12"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="Register" Click="RegisterButton_Click"
                    Padding="24,8" Margin="0,0,8,0" IsDefault="True">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Setter Property="Background" Value="#4CAF50"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Grid.Column="2" Content="Cancel" Click="CancelButton_Click"
                    Padding="24,8" IsCancel="True">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Setter Property="Background" Value="#E0E0E0"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>
